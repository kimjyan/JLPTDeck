# Fastlane plugin dependencies. Pulled in by the root `Gemfile` via
# `eval_gemfile`. Auto-generated by `fastlane add_plugin <name>` but
# committed manually here so first-time clones can `bundle install`
# without an extra step.

gem "fastlane-plugin-firebase_app_distribution", "~> 0.10"
